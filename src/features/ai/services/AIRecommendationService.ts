/**
 * Re-export the AIRecommendationService interface.
 * Consumers should import from here so the concrete implementation can be swapped at the service layer.
 */
export type { AIRecommendationService } from '../types';
